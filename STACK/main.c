#include <stdio.h>
#include <stdlib.h>
#include <stack.h>

int main(void)
{
	list* stack = NULL;
	int value;
	printf("Input value and press enter. \n");
	scanf( "%d", &value );
	push(list* stack, value);
	push(list* stack, value);
	pop(list* stack);
	
}